# mruil.github.io

Personal homepage of Yang Liu — https://mruil.github.io

Static site (no build step): `index.html` + `assets/site/{style.css, main.js, data.js}`.
Publications and news are edited in `assets/site/data.js`.

The previous Jekyll (bay theme) version is preserved on the `legacy-bay-theme` branch.
